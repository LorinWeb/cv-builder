import type { ResumeSourceData } from '../../data/types/resume';
import type { ResumeStudioWarning } from './types';

export function getResumeStudioWarnings(
  _data: ResumeSourceData
): ResumeStudioWarning[] {
  return [];
}
